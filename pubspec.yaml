name: badminton_fund_tracker

description: Badminton Fund Tracker App
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^3.13.0
  firebase_database: ^11.3.5
  provider: ^6.1.2
  url_launcher: ^6.3.0
  android_intent_plus: ^5.3.0
  share_plus: ^10.0.0

flutter:
  uses-material-design: true